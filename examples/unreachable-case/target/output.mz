main() :=
    match >(0u64, call "mz_ffi_limit" ()) {
    F() -> f0(call "mz_ffi_limit" (), 1u64),
    T() -> Panic("no such pattern `Unreachable`")
    };

f0(v0, v1) :=
    match >(v1, v0) {
    F() -> f0(call "mz_ffi_limit" (), +(v1, 1u64)),
    T() -> Done()
    };
